# Music League Bot — Backlog

Items deferred from active sprints. Pull into a sprint when ready.

---

## Deferred from sprint-8 / digest planning session (2026-05-18)

### Link converter → song action button
**What:** The `/link` route (link converter) has been removed from the sidebar nav. The feature itself still exists at `/link` but is no longer reachable from the main nav.

**Next step:** Add a "Convert link" action button directly on individual song rows (shortlist, chat watcher, round submissions) so the tool is accessible in context rather than as a standalone nav destination.

**Why deferred:** Digest preview sprint is the current priority. The nav removal is done; the contextual button is a polish task that can follow after digest ships.

---
